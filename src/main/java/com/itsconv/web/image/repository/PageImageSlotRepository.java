package com.itsconv.web.image.repository;

import com.itsconv.web.image.domain.PageImageSlot;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PageImageSlotRepository extends JpaRepository<PageImageSlot, Long> {
}
